<Window x:Class="ToggleButtons.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToggleButtons"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="1000">
    <Window.Resources>
        <ResourceDictionary >
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ToggleBtnStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ToggleButton x:Name="TB1" Grid.Column="0" Margin="10" Style="{StaticResource SquareInnerCircleSwitch}"/>
        <ToggleButton x:Name="TB2" Grid.Column="1" Margin="10" Style="{StaticResource RectangleHorizontalSwitch}"/>
        <ToggleButton x:Name="TB3" Grid.Column="2" Margin="10" Style="{StaticResource CircleSwitch}"/>
    </Grid>
</Window>
